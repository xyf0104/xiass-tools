# Cloudflare R2 Automatic Update Progress

## 2026-07-14

- Created an isolated plan so the R2 work does not overwrite the active codebase-modularization plan or the root Panda/WiX history.
- Inventoried current Tauri configuration, Rust/JavaScript dependencies, Settings update UI, Windows Burn artifacts, and macOS GitHub Actions workflow.
- Confirmed the current app only checks GitHub release metadata and has no signed self-update implementation.
- Confirmed the official Tauri updater and Cloudflare R2 production contracts, including signed artifacts, custom-domain delivery, and S3-compatible publication.
- Chose a stable-only first channel, immutable versioned release objects, a mutable manifest published last, MSI-based Windows self-updates, and app-archive macOS self-updates.
- Added `docs/r2-auto-update.md` with the object layout, publishing order, cache policy, security boundary, platform contracts, and required production inputs.
- Recorded the decision to exclude incremental and differential updates from the initial implementation; Windows and macOS will use complete signed update packages.
- Started Phase 2 after the user requested immediate Tauri updater integration. The implementation will keep unconfigured development builds usable and enable signed self-update behavior in explicitly configured release builds.
- Completed Phase 2: registered updater/process plugins, added least-privilege capabilities, switched configured desktop builds to signed updater checks, and added localized download/install/relaunch progress with duplicate-install suppression.
- Completed Phase 3: added environment-generated updater configuration, integrated signed updater builds into Windows Burn and macOS CI, and verified a real test-key MSI build produced three `.msi.sig` files without committing signing material.
- Completed Phase 4: added a manifest generator plus an AWS CLI R2 publisher with dry-run support, SHA-256 immutable-object validation, manifest-last publication, and rollback through a previous manifest.
- Verification so far: focused updater tests passed, Svelte check passed, Cargo check passed, a signed MSI updater build passed, manifest generation from the real MSI signature passed, and the R2 publisher dry run passed.
- Final verification passed: 179 unit tests, Svelte check with 0 errors and 0 warnings, Cargo check, normal production build without updater variables, signed MSI updater build with a throwaway key, manifest generation, R2 dry-run publication, and whitespace checks.
- Review found and fixed unconfigured-build behavior, duplicate install triggering, and size-only immutable object validation. Settings visual QA remains manual because the mandated in-app browser runtime failed to initialize with `Cannot redefine property: process`.
- Completed Phase 6 after the user chose R2-only updates: removed the GitHub Releases API fallback and constant, made unconfigured builds network-quiet, and added localized unconfigured status copy.
- Started Phase 7 after the user rejected a GitHub dependency: restored the existing GitHub macOS workflow to its ordinary non-updater build, added provider-independent platform updater build commands, and rewrote release configuration around generic secure environment variables.
- Completed Phase 7: all 180 unit tests passed, Svelte check reported 0 errors and 0 warnings, the production frontend build passed, Cargo check passed, and a generated updater config pointed only to the R2 custom-domain manifest endpoint.
- Started Phase 8 after the user identified that the updater was not necessarily connected to R2. A red regression test proved the Windows updater alias silently fell back to an updater-disabled build, then the build command was changed to require complete updater inputs.
- Completed Phase 8: the unconfigured Windows updater command now fails before compilation, configured generation writes the expected `/stable/latest.json` endpoint and public key, all 180 unit tests pass, and Svelte check remains clean. The local machine currently has no updater signing key files or persisted updater/R2 environment configuration, so a real R2 release cannot be produced until production values are supplied.
- Started Phase 9 after receiving the production R2 domain. HTTPS and Cloudflare routing were verified, and the domain was moved into a checked-in public updater configuration shared by Vite, the Tauri config generator, and Windows release builds.
- Completed Phase 9: with only a temporary verification public key, config generation produced `https://download.codestudio.build/stable/latest.json`; all documentation examples now use the production domain. The live endpoint remains 404 until the first signed manifest is uploaded, and the checked-in public key remains empty until a production signing key is generated.
- Post-interruption verification passed: the Vite production build accepts the checked-in updater config, and `updater:build:windows` stops before compilation while the production signing public/private keys are absent. No updater-disabled release was created.
- Completed Phase 10: generated the production updater key with a random password, stored the password using CurrentUser DPAPI under a user-only ACL, embedded only the public key in `updater.config.json`, and added PBKDF2-HMAC-SHA256 + AES-256-CBC + HMAC-SHA256 portable export/import scripts. A real migration round trip preserved private key, public key, and signing password exactly. The production Windows updater build generated three signed MSI files, three signatures, and the verified Burn installer; manifest generation and R2 publication dry-run both used `download.codestudio.build`.
- Completed Phase 11: final Windows and macOS release artifacts are normalized to kebab-case, manifest generation and R2 publication reject spaces/underscores, all existing Windows bundle outputs were normalized, and the desktop `latest.json` now references `CodeStudio-Lite-1.4.1-x64-en-US.msi`. Full verification passed with 182 unit tests and a clean Svelte check.
- Started Phase 12 after the artifact naming contract was extended to place the operating-system token between version and architecture. The scope covers Windows, macOS, and Linux package names while retaining Tauri platform identifiers and R2 directory keys unchanged.
- Implemented Phase 12 naming: Windows MSI/NSIS/Burn outputs now include `Windows`, macOS outputs include `macOS`, and new Linux build/normalization commands cover AppImage, updater archives, DEB, RPM, and adjacent signatures with a `Linux` token.
- Manifest generation and R2 publication now validate the exact `CodeStudio-Lite-<version>-<OS>-` prefix and support an explicit Linux Tauri platform key. The focused regression suite passed all 183 tests.
- Rebuilt the 1.4.1 Burn installer from existing signed MSI inputs without recompiling Tauri. Burn verification passed, all current Windows artifacts were migrated to the new names, and the desktop `latest.json` was regenerated with the platform-qualified MSI URL.
- Completed Phase 12: R2 dry-run publication used the new Windows object key, the Linux normalizer was exercised against AppImage/signature/DEB/RPM fixtures, all 184 unit tests passed, Svelte check reported 0 errors and 0 warnings, script syntax checks passed, `git diff --check` passed, and all current 1.4.1 Windows artifact names matched the new contract.
